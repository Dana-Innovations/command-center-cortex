{
  "mcpServers": {
    "powerbi": {
      "command": "npx",
      "args": ["tsx", "powerbi-mcp-server/src/index.ts"],
      "cwd": "/Users/aris/PROJECTS/command-center",
      "env": {
        "AZURE_TENANT_ID": "${AZURE_TENANT_ID}",
        "AZURE_CLIENT_ID": "${AZURE_CLIENT_ID}",
        "AZURE_CLIENT_SECRET": "${AZURE_CLIENT_SECRET}"
      }
    }
  }
}
